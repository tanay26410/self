#next permutation
from itertools import permutations
def permut(nums):
    n=''.join(nums)
    n=int(n)
    return n
    # perm=permutations(nums)
    # for i in perm:
        
    #         print(i)
    #         break
nums=[1,1,5]
print(permut(nums))